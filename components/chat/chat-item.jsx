"use client";

import { Member, MemberRole, Profile } from "@prisma/client";

export const ChatItem = ({
  id,
  content,
  member,
  timestamp,
  fileUrl,
  deleted,
  currentMember,
  isUpdated,
  socketUrl,
  socketQuery
}) => { 
    <div>
        chat item
    </div>
}