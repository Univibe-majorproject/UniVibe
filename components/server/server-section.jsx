"use client";
import { ChannelType, MemberRole } from "@prisma/client";


export const ServerSection = ({
    label, 
    role, 
    sectionType,
    ChannelType,
    server,
}) =>{
    return (
        <div>
            Server Section
        </div>
    )
}